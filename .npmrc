# This dependency tree (Privy + Coinbase CDP SDK + Porto + wagmi + Solana kit v6 +
# web3.js v1) has unavoidable peer-dependency conflicts; npm's strict resolver
# ERESOLVE-fails without this. Lets a plain `npm ci` / `npm install` succeed.
legacy-peer-deps=true
